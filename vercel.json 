{
  "version": 2,
  "projects": [
    {
      "name": "client",
      "root": "apps/client",
      "builds": [
        {
          "src": "package.json",
          "use": "@vercel/static-build",
          "config": {
            "distDir": "dist"
          }
        }
      ],
      "routes": [
        {
          "src": "/(.*)",
          "dest": "/dist/$1"
        }
      ]
    },
    {
      "name": "api",
      "root": "apps/api",
      "builds": [
        {
          "src": "package.json",
          "use": "@vercel/node"
        }
      ],
      "routes": [
        {
          "src": "/api/(.*)",
          "dest": "src/app.ts"
        }
      ]
    }
  ]
}
